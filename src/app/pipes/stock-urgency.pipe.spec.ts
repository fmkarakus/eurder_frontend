import { StockUrgencyPipe } from './stock-urgency.pipe';

describe('StockUrgencyPipe', () => {
  it('create an instance', () => {
    const pipe = new StockUrgencyPipe();
    expect(pipe).toBeTruthy();
  });
});
